### Quick Start

run the below commeands from your cli:

```bash
cd path/to/project
yarn
yarn serve

# OR

cd path/to/project
npm install
npm run serve
```

### Fix ESLint command

npm run lint -- --fix
